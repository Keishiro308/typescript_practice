const binary = 0b1010;

const octal = 0o755;

const hexademical = 0xff;

const big = 1e8;

const small = 4e-5;

console.log(binary, octal, hexademical, big, small);

const bignum: bigint = (123n + 456n) * 2n;

console.log(bignum);

const backquote_message: string = `Hello, 

World!`;

console.log(backquote_message);